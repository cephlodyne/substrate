{
  "name": "CEPHLODYNE_APP_NAME-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "{{PROTOBUF_VERSION}}",
    "@sveltejs/vite-plugin-svelte": "{{VITE_PLUGIN_SVELTE_VERSION}}",
    "@tsconfig/svelte": "{{TSCONFIG_SVELTE_VERSION}}",
    "svelte": "{{SVELTE_VERSION}}",
    "svelte-check": "{{SVELTE_CHECK_VERSION}}",
    "typescript": "{{TYPESCRIPT_VERSION}}",
    "vite": "{{VITE_VERSION}}"
  },
  "dependencies": {
    "@bufbuild/protobuf": "{{PROTOBUF_VERSION}}",
    "@connectrpc/connect": "{{CONNECT_WEB_VERSION}}",
    "@connectrpc/connect-web": "{{CONNECT_WEB_VERSION}}"
  }
}
